# sem fixtures

`sem-verbose.json` is a representative aggregate fixture maintained against
the verbose JSON envelope emitted by the latest supported `sem` release.
Revalidate its shape with real command output whenever that release changes,
then retain representative entries for every semantic change type supported by
SDV.
