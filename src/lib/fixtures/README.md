# sem fixtures

`sem-0.7-verbose.json` follows the verbose JSON envelope emitted by sem 0.7.0.
Its field set is based on the upstream 0.7.0 serializer and JSON assertion at
commit `d61f5ef`. The fixture expands that shape with one representative entry
for every semantic change type supported by SDV.
